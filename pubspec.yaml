name: web_smooth_scroll
description: A package to help you provide a smoother and aesthetic scroll feeling.
version: 0.0.3
homepage: https://github.com/mayurnile/web_smooth_scroll

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  plugin: 
    platforms: 
      web: 